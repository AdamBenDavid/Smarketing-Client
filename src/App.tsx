import React from "react";
import Page from "./components/Page";

const App: React.FC = () => {
  return (
    <div style={styles}>
      <Page />
    </div>
  );
};

const styles = {
  margin: 0,
  padding: 0,
};

export default App;
